apiVersion: v1
kind: Service
metadata:
  name: zenwebapi10-service
  labels:
    app: zenwebapi10
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: zenwebapi10
